# breastCancerDiagnosisUsingKNN
A machine learning project that uses the K-Nearest Neighbors (KNN) algorithm to classify breast cancer tumors as malignant or benign using the Wisconsin Breast Cancer dataset. Includes data preprocessing, hyperparameter tuning, and performance evaluation using accuracy, precision, recall, and confusion matrix visualization.
